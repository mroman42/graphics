void main() {
  // El objetivo es escribir la variable:
  //  - gl_FragColor, color a asignar al pixel
  //
  // Puede leerse la variable:
  //  - gl_Color, color de la primitiva
  gl_FragColor = gl_Color;
}